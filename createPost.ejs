<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create a Post in <%= subName %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
        }
        h1 {
            color: red;
            text-align: center;
            margin-bottom: 40px;
        }
        .container {
            width: 80%;
            display: flex;
            justify-content: space-between;
        }
        .left, .right {
            width: 45%;
        }
        .left {
            text-align: left;
        }
        .right {
            text-align: right;
        }
        input[type="text"] {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            margin-bottom: 20px;
        }
        textarea {
            width: 100%;
            height: 300px;
            padding: 10px;
            font-size: 16px;
        }
        button {
            background-color: black;
            color: white;
            padding: 10px 20px;
            font-size: 16px;
            border: none;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <h1>Create a post for <%= subName %></h1>
    <!-- The form now points to the submittedPost route -->
    <form action="/<%= subName %>/submittedPost" method="POST">
        <div class="container">
            <div class="left">
                <label for="postTitle">Input a title for your post:</label>
                <input type="text" id="postTitle" name="postTitle" required>
            </div>
            <div class="right">
                <label for="postContent">Write your post!</label>
                <textarea id="postContent" name="postContent" required></textarea>
            </div>
        </div>
        <div style="margin-top: 20px;">
            <button type="submit">Submit Post</button>
        </div>
    </form>
</body>
</html>